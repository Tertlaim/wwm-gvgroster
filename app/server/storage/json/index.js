// storage/json/index.js - Barrel export for JSON file storage
const data = require('./data');
const history = require('./history');
const auth = require('./auth');

module.exports = { data, history, auth };
