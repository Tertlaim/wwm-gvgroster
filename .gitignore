# Sensitive / live data (recreated automatically on server start).
# These live under app/ but are NOT web-served: server.js only mounts
# /css, /js and /vendor on express.static.
app/config/auth.json
app/data/

# Environment config
.env

# Dependencies
node_modules/
package-lock.json

# Logs
*.log
.kilo/
app/js/panel-fix.js

# Tooling artifacts
.playwright-mcp/
